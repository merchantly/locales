en:
  services:
    invite:
      sms_text: "You were invited to %{vendor}, to register - %{url}"
    vendor_registration:
      invalid_phone_code: Invalid PIN
      operator_name: "Operator of store %{vendor}"
      first_category: Root category
      cash_on_delivery: C.O.D
      cash_on_selfdelivery: Cash on delivery
      payment_w1: "Online payment (Visa/MasterCard, QIWI, etc.)"
      delivery: 'Post delivery'
      product_name: 'Product Name'
      product_description: 'Product description'
      default_cashier: 'Default'
      default_branch: 'Default'
      default_product_vat_group: 'Default'
      default_cash: 'Cash'
      default_pickup: 'Pickup'
      default_card: 'Card'
    pin_sender:
      sms_text: 'Your PIN to enter: %{pin}'
    client_pin_sender:
      sms_text: '%{pin} Your PIN code to log into your account %{cabinet_url}'
    tariff_payment:
      fee:
        per_month:
          description: 'Subscription fee for %{date} - %{amount}'
        orders:
          description: '%{amount} per orders  (%{per_order_amount} per order, %{orders_count}). %{date}'
        sms:
          description: '%{amount} for SMS (%{per_sms_amount} for 1 SMS, all sent messages %{sms_count} SMS). %{date}'
        moysklad:
          description: '%{amount} for integration with MyStore service'

    vendor_notifications:
      partner_incoming:
        sms_text: '%{name}, partner balance in kiiosk.ru replenished by %{amount}.  Current balance: %{current_amount}'
      not_enough_sms_money:
        sms_text: '%{url}: Not enough funds to send SMS'
      sms_money_limit_reached:
        sms_text: '%{url}: Your SMS balance is approaching the shutdown threshold. Balance: %{amount}'
      unpublish:
        sms_text: '%{url}: Store unpublished'
      negative_balance:
        sms_text: '%{url}: your balance: %{balance}. With a negative balance, sending SMS will not be available.'
      balance_refill:
        sms_text: '%{url}: Your balance has been replenished by %{amount}. Current balance: %{current_amount}'
      balance_subtract:
        sms_text: '%{url}: A charge of %{amount} - %{details}. Current balance: %{current_amount}'
      need_payment:
        sms_text: '%{url}: The paid service period ends %{unpublish_date}. Total: %{invoice_amount} - %{payment_link}'

    vendor_smtp_verification:
      steps:
        connection: 'Server Connection'
        encryption: 'Encryption Setup'
        authentication: 'Authentication'
        send_test: 'Test Email Delivery'
      messages:
        connection_established: 'Connected to %{host}:%{port}'
        encryption_not_required: 'No encryption required'
        ssl_active: 'SSL/TLS encryption enabled'
        starttls_active: 'STARTTLS encryption enabled'
        auth_success: 'Authentication successful for %{username}'
        test_sent: 'Test email sent to %{email}'
      errors:
        connection_timeout: 'Connection to %{host}:%{port} timed out'
        connection_refused: 'Connection refused by %{host}:%{port}'
        host_unreachable: 'Host %{host} is unreachable'
        ssl_error: 'SSL error: %{message}'
        dns_error: 'DNS error: %{message}'
        connection_error: 'Connection error: %{message}'
        starttls_not_supported: 'Server does not support STARTTLS'
        starttls_error: 'STARTTLS error: %{message}'
        auth_error: 'Authentication error: %{message}'
        server_rejected: 'Server rejected: %{message}'
        server_busy: 'Server busy: %{message}'
        send_timeout: 'Send operation timed out'
        unexpected: 'Unexpected error: %{message}'
      hints:
        connection_timeout:
          wrong_host: 'Check the SMTP server address'
          wrong_port: 'Try a different port (25, 465, 587, 2525)'
          firewall: 'Connection may be blocked by a firewall'
          server_unavailable: 'Server may be temporarily unavailable'
        connection_refused:
          not_accepting: 'SMTP server is not accepting connections on this port'
          try_other_port: 'Try a different port'
        host_unreachable:
          check_address: 'Check the server address'
          temporary: 'This may be a temporary network issue'
          check_dns: 'Check the domain DNS settings'
        ssl_error:
          encryption_mismatch: 'Encryption settings do not match server requirements'
          port_encryption: 'Port 465 usually requires SSL, port 587 requires STARTTLS'
          try_change: 'Try changing the encryption mode'
          self_signed: 'Disable SSL verification if server uses self-signed certificate'
        auth_failed:
          wrong_credentials: 'Check username and password'
          app_password: 'Some services (Gmail, Yandex) require app password'
          two_factor: 'Use app password with two-factor authentication'
          less_secure: 'Ensure app access is allowed in email settings'
        sender_rejected:
          not_allowed: 'Server does not allow sending from this address'
          same_email: 'Sender address usually must match username'
          aliases: 'Check alias settings in email service'
        recipient_rejected:
          check_email: 'Check recipient email address'
        server_busy:
          try_later: 'Try again in a few minutes'
        send_timeout:
          slow_response: 'Server is responding too slowly'
          increase_timeout: 'Try increasing timeout in advanced settings'
          server_overloaded: 'Server may be overloaded'
        connection_error:
          unknown: 'Unknown connection error occurred'
          check_settings: 'Check all SMTP settings'
          contact_support: 'Contact email service support'
        unexpected:
          retry: 'Try verifying again'
          contact_support: 'Contact support if error persists'
      test_email:
        subject: 'Test email from %{vendor}'
        body: |
          This is a test email from "%{vendor}".

          If you received this email, SMTP settings are working correctly.

          Connection settings:
          - Server: %{host}
          - Port: %{port}
          - Sender: %{from_email}
